import React from 'react'
import ReactDOM from 'react-dom'

// TODO: have this css file in a link tag in index.html to avoid large output from webpack (or gitignore it)
import 'semantic-ui-css/semantic.min.css'

ReactDOM.render(
  <div>Testing</div>,
  document.getElementById('app')
)
